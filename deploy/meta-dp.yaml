apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: metagpu-device-plugin
  namespace: kube-system
spec:
  selector:
    matchLabels:
      name: metagpu-device-plugin
  template:
    metadata:
      annotations:
        scheduler.alpha.kubernetes.io/critical-pod: ""
      labels:
        name: metagpu-device-plugin
    spec:
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      priorityClassName: "system-node-critical"
      imagePullSecrets:
      - name: regcred
      containers:
        - name: fractor
          image: docker.io/cnvrg/metagpu-device-plugin:latest
          command:
            - /usr/bin/fractor-device-plugin
            - start
            - -c
            - /etc/fractor
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ "ALL" ]
          volumeMounts:
            - name: device-plugin
              mountPath: /var/lib/kubelet/device-plugins
            - name: config
              mountPath: /etc/fractor
      volumes:
        - name: device-plugin
          hostPath:
            path: /var/lib/kubelet/device-plugins
        - name: config
          configMap:
            name: fractor-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fractor-config
  namespace: kube-system
data:
  config.yaml: |
    metaGpus: 10
    accelerator: nvidia
    deviceCacheTTL: 3600
    resourceName: cnvrg.io/metagpu
